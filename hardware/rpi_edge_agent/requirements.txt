# Omni-Sense Raspberry Pi Edge Agent — Python dependencies
# Install with:  pip install -r requirements.txt

# MQTT client (widely available on Pi, no async runtime required)
paho-mqtt>=1.6.1,<3.0

# I2C communication with ADXL345 (smbus2 is pure-Python, no C extension needed)
smbus2>=0.4.3

# Optional: richer system telemetry (battery, CPU temp via psutil)
psutil>=5.9.0

# Optional: real 16 kHz audio from USB microphone instead of accelerometer
# Uncomment if you wire a USB mic or I2S mic and set USE_SOUNDDEVICE=1
# sounddevice>=0.4.6
# numpy>=1.24.0
