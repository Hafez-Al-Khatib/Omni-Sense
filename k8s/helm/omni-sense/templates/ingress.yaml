{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: omni-sense-ingress
  namespace: {{ include "omni-sense.namespace" . }}
  labels:
    {{- include "omni-sense.labels" . | nindent 4 }}
    app.kubernetes.io/component: ingress
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    # EEP REST API — used by field apps and external integrations
    - host: {{ .Values.ingress.hosts.api.host }}
      http:
        paths:
          - path: {{ .Values.ingress.hosts.api.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.eep.name }}
                port:
                  name: http
    # Ops Console (Streamlit) — internal operators
    - host: {{ .Values.ingress.hosts.console.host }}
      http:
        paths:
          - path: {{ .Values.ingress.hosts.console.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.omniPlatform.name }}
                port:
                  name: http
{{- end }}
