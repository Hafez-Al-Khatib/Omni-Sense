apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.iep2.name }}
  namespace: {{ include "omni-sense.namespace" . }}
  labels:
    {{- include "omni-sense.componentLabels" (dict "component" .Values.iep2.name "context" .) | nindent 4 }}
spec:
  replicas: {{ .Values.iep2.replicas }}
  selector:
    matchLabels:
      {{- include "omni-sense.selectorLabels" (dict "component" .Values.iep2.name "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "omni-sense.componentLabels" (dict "component" .Values.iep2.name "context" .) | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: {{ .Values.iep2.name }}
          image: {{ include "omni-sense.image" (dict "repository" .Values.iep2.image.repository "context" .) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.iep2.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: omni-sense-config
          readinessProbe:
            httpGet:
              path: {{ .Values.iep2.readinessProbe.path }}
              port: {{ .Values.iep2.readinessProbe.port }}
            initialDelaySeconds: {{ .Values.iep2.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.iep2.readinessProbe.periodSeconds }}
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: {{ .Values.iep2.livenessProbe.path }}
              port: {{ .Values.iep2.livenessProbe.port }}
            initialDelaySeconds: {{ .Values.iep2.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.iep2.livenessProbe.periodSeconds }}
            failureThreshold: 5
          resources:
            {{- toYaml .Values.iep2.resources | nindent 12 }}
      restartPolicy: Always
