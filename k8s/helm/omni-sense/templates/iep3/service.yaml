apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.iep3.name }}
  namespace: {{ include "omni-sense.namespace" . }}
  labels:
    {{- include "omni-sense.componentLabels" (dict "component" .Values.iep3.name "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "omni-sense.selectorLabels" (dict "component" .Values.iep3.name "context" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.iep3.port }}
      targetPort: http
      protocol: TCP
---
# PVC for ticket storage (shared across rolling updates via ReadWriteOnce)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: iep3-tickets-pvc
  namespace: {{ include "omni-sense.namespace" . }}
  labels:
    {{- include "omni-sense.componentLabels" (dict "component" .Values.iep3.name "context" .) | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  {{- if .Values.timescaledb.persistence.storageClass }}
  storageClassName: {{ .Values.timescaledb.persistence.storageClass }}
  {{- end }}
